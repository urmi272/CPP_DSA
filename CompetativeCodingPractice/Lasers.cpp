#include <iostream>
using namespace std;
int main() {
    int t;
    cin >> t;
    while (t--) {
        int n, m, x, y;
        cin >> n >> m >> x >> y;
        int originalN = n;
        int originalM = m;
        while (n--) {
            int a;
            cin >> a;
        }
        while (m--) {
            int b;
            cin >> b;
        }
        cout << (originalN + originalM) << endl;
    }
    return 0;
}